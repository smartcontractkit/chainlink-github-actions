{
  "name": "@smartcontractkit/github-app-token-issuer-action",
  "version": "0.1.0-5136e7a",
  "description": "The github action for interfacing with the token issuer service",
  "main": "dist/index.js",
  "files": [
    "dist/index.js",
    "action.yml"
  ],
  "scripts": {
    "clean": "tsc -b --clean",
    "build:lint": "tsc -b",
    "build": "esbuild index.ts --minify --bundle --platform=node --outfile=dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.15.13"
  },
  "dependencies": {
    "@smartcontractkit/github-app-token-issuer-signed-request": "workspace:*",
    "@actions/core": "^1.10.0"
  }
}
